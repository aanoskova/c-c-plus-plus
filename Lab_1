Лабораторная работа №1
Определите можно ли из отрезков с длинами a,b,c построить треугольник. Если можно,то выяснить тип треугольника (разносторонний, равнобедренный, равносторонний)

 #include <stdio.h>

int main() {
    int a, b, c;
    printf("Введите длину отрезка a: ");
    scanf_s("%d", &a);
    printf("Введите длину отрезка b: ");
    scanf_s("%d", &b);
    printf("Введите длину отрезка c: ");
    scanf_s("%d", &c);

    if (a + b > c && a + c > b && b + c > a) {
        if (a == b && b == c) {
            printf("Треугольник равносторонний\n");
        }
        else if (a == b || a == c || b == c) {
            printf("Треугольник равнобедренный\n");
        }
        else {
            printf("Треугольник разносторонний\n");
        }
    }
    else {
        printf("Треугольник с такими сторонами нельзя построить\n");
    }

    return 0;
}
