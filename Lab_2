Лабораторная работа № 2
Дан одномерный массив А из N элементов. Преобразовать массив, увеличив все отрицательные числа, содержащиеся в массиве,
на исходное значение первого отрицательного числа. Если отрицательные числа B массиве отсутствуют, то оставить массив без изменений.

#include <locale.h>
#include <stdio.h>

int main(void) {
    setlocale(LC_ALL, "Russian");
    int A[100], N, fN = 0, foN = 0;

    int i = 0;

    printf("Введите размер массива (<100): ");
    scanf_s("%d", &N);

    // Проверка на корректный ввод размера массива
    if (N <= 0 || N >= 100) {
        printf("Размер массива должен быть больше 0 и меньше 100.\n");
        return 1; // Возвращаем ошибку
    }

    while (i < N)
    {
        printf("Введите %d элемент массива: ", i + 1);
        scanf_s("%d", &A[i]);

        // Поиск первого отрицательного числа
        if (A[i] < 0 && !foN)
        {
            fN = A[i];
            foN = 1;
        }
        i++;
    }

    // Если найдено отрицательное число
    if (foN)
    {
        i = 0;
        while (i < N)
        {
            if (A[i] < 0)
            {
                A[i] += fN;
            }
            i++;
        }
    }

    printf("Преобразованный массив:\n");
    i = 0;
    while (i < N)
    {
        printf("%d ", A[i]);
        i++;
    }
    printf("\n");

    return 0; // Возвращаем 0 для успешного завершения
}
